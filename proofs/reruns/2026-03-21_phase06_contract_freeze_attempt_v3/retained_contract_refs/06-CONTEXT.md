> Retained context copy for the March 21 blocker packet. Original machine-local
> paths are intentionally preserved in this copied reference; do not treat this
> file as the current public instruction surface for the repo.

# Phase 06 Context: Open-Access Enterprise Wedge Benchmark

## What This Phase Is

Phase 06 is the sovereign proof gate for the GitHub-first, source-available FT product. Its job is to freeze a declared open-access or otherwise commercially safe benchmark suite, define the real incumbent-workflow query catalog and truth surface, and rerun the sidecar package honestly.

This phase is not a private-buyer sales exercise. It is the public enterprise benchmark closure for the first commercial wedge.

## What It Is Intended For

- Lock the exact corpus suite, provenance, and reuse rights for the public proof lane.
- Lock the exact query-by-example workload that represents a real incumbent finance workflow.
- Lock the labeled or auditable truth surface for `FT-C004`.
- Run the sovereign metric package against the declared baseline matrix and close pass, fail, or kill without narrative padding.

## What Is Already Obvious From The Charter And Conventions

- The first artifact remains the finance-native sidecar accelerator, not a broad TSDB replacement.
- The hard guardrails stay unchanged: compression, fidelity, retrieval quality, latency, and roundtrip integrity all remain binding.
- `FT-C004` still requires labeled or auditable truth; unlabeled retrieval is non-authority.
- The proof must stay sidecar-shaped and benchmarked against real incumbent workflows and raw-scan baselines.
- GitHub/source-available distribution is the mainline delivery model, so the decisive benchmark inputs must be legally reusable or commercially safe for that path.

## What Is Still Missing

- The exact open-access or commercially safe corpus suite for Phase 06 is not frozen in the repo.
- The exact labeled or auditable truth surface for `FT-C004` is not frozen in the repo.
- The futures/FX slice is not yet closed by the current Alpaca delayed-stock tooling.
- The baseline matrix and replay/export path need to be frozen against real incumbent workflows rather than private-buyer shorthand.

## Why This Phase Cannot Be Closed Yet

The governing objective is clear, but the decisive benchmark inputs are not yet frozen. Without a declared corpus suite, truth surface, and baseline matrix, Phase 06 would collapse into assertion instead of proof.

## Honest Closure Plan

1. Freeze the declared Phase 06 corpus suite with provenance, rights, schema, and hash manifests.
2. Freeze the query catalog, incumbent baseline matrix, and replay/export destination.
3. Freeze labeled or auditable `FT-C004` truth.
4. Run the sovereign metric package and close the sidecar thesis as pass, fail, or kill.
