# ZPE-FT Agent Handover

Timestamp (UTC): `2026-04-25T12:51:11Z`

## 1. Governing Reality

Do not reward hack this lane.

The sovereign acceptance gate for ZPE-FT is still Phase 06 closure, not Wave 1.5b hygiene. This branch improves investor/tester readiness at the package, CI, README, and drift-control surface. It does **not** close the open-access enterprise benchmark.

Phase 06 remains blocked on the same authority gaps as before:

- exact `30`-symbol authority OHLCV pack absent
- exact `3`-series top-of-book authority pack absent
- auditable `FT-C004` truth unresolved
- public/proxy rehearsal surfaces remain non-authority

Primary blocker artifacts:

- `proofs/reruns/2026-03-21_phase06_contract_freeze_attempt_v3/missing_inputs_packet.json`
- `proofs/phase06_inputs/series_gap_matrix.csv`
- `proofs/artifacts/real_market_benchmarks/ft_c004_truth_requirements.json`

## 2. Current Git / PR State

Repo:

- `https://github.com/Zer0pa/ZPE-FT`

Current local branch:

- `chore/wave-1-5b-investor-readiness-2026-04-25`

Branch head:

- `9b30736e8c1cfd899ccdca019d6c0ef5fa325361`

Open PR for this branch:

- PR `#19`
- URL: `https://github.com/Zer0pa/ZPE-FT/pull/19`
- Title: `chore(ft): Wave 1.5b investor-readiness hardening`
- State at handover: `OPEN`, not draft, all GitHub checks green

Important stack note:

- PR `#18` is still open: `https://github.com/Zer0pa/ZPE-FT/pull/18`
- PR `#19` already includes the PEP 639 classifier fix by cherry-picking the equivalent patch into commit `fe9fd58a32cb607401798a7115a2b964e34d7c53`
- If owner merges `#19` first, PR `#18` is likely superseded and should be reviewed before merging blindly
- If owner merges `#18` first, PR `#19` stays valid but its diff vs `main` will shrink

## 3. What This Branch Changed

The branch contains two commits:

1. `fe9fd58a32cb607401798a7115a2b964e34d7c53`
   - `chore(pyproject): remove License classifier (PEP 639 conflict with license expression)`
2. `9b30736e8c1cfd899ccdca019d6c0ef5fa325361`
   - `chore(ft): Wave 1.5b investor-readiness hardening`

The material changes in `9b30736...` are:

- `pyproject.toml`
  - bounded `setuptools` to `>=68,<82`
  - removed unused `market` and `docs` optional dependency groups
  - added `duckdb` and `pandas` to `.[test]` because the test suite actually imports and executes code that requires them
- `README.md`
  - removed exact ratio claims and other claims not backed by both retained proof artifacts and CI coverage
  - replaced those with proof-and-test-backed verified-claim rows
  - kept the honest commercial wedge and explicit blocked Phase 06 status
- deleted drift
  - stale cross-lane note: `docs/INTEGRATION_PATTERN.md`
  - machine-local env-check logs under `poc/logs/`
  - duplicate public benchmark artifact files:
    - `proofs/PHASE3_PUBLIC_BENCHMARKS.json`
    - `proofs/artifacts/public_benchmarks/phase3_public_benchmarks_remote.*`

No changes were made to:

- `LICENSE`
- `CITATION.cff`
- `.zenodo.json`
- `SECURITY.md`
- `REPRODUCIBILITY.md`
- package version
- `.github/workflows/*`

## 4. Why These Changes Were Necessary

The broken state on `origin/main` before this branch was:

- `ci` failed because editable install hit the deprecated license classifier / PEP 639 conflict
- `package-verification` failed for the same reason
- once the license-classifier blocker was conceptually removed, the base-package verification surface was still incomplete because `.[test]` did not install dependencies that the current tests actually need:
  - `tests/test_phase06_partial_scripts.py` imports `pandas`
  - `tests/test_public_benchmarks.py` executes `scripts/run_public_market_benchmarks.py`, which imports `pandas` and uses `duckdb`

README also carried exact metrics and metadata that CI did not assert. Those were claim drift and were removed rather than retrofitted.

## 5. Verification Completed

### Local verification

All of the following were run and passed before push:

- base test surface:
  - `python -m pip install -e '.[test]'`
  - `python -m pytest -q`
- dev/CI surface:
  - `python -m pip install -e '.[dev]'`
  - `python -m pytest -q`
- proof extras:
  - `python -m pip install -e '.[test,proof]'`
  - `python poc/scripts/00_env_check.py --workspace . --repo .`
- observability extras:
  - `python -m pip install -e '.[observability]'`
  - import check for `comet_ml` and `opik`
- native helper:
  - `python -m pip install -e '.[test,native]'`
  - `cd core && maturin develop --release`
  - `python -m pytest -q tests/test_codec_core.py tests/test_packet_roundtrip.py`
- build:
  - `python -m build`

### Remote verification

PR `#19` GitHub checks are all green:

- `ci`
  - `test (3.11)` PASS
  - `test (3.12)` PASS
- `package-verification`
  - `base-package (3.11)` PASS
  - `base-package (3.12)` PASS
  - `proof-extras` PASS
  - `observability-extras` PASS
  - `native-helper` PASS
- `CodeQL`
  - `Analyze (actions)` PASS
  - `Analyze (python)` PASS
  - `Analyze (rust)` PASS
  - `CodeQL` PASS

## 6. Exact Scope of the Diff

Compared with `origin/main`, this branch currently changes only:

- `README.md`
- `pyproject.toml`
- deletions of stale drift files listed above

No source feature work was added.

## 7. Residual Issues / Live Decision Points

### A. Still blocked at the sovereign gate

Wave 1.5b is green. Phase 06 is still blocked.

Do not let the green CI / cleaner README surface get misreported as lane closure.

### B. Residual repo baggage still present

`scripts/run_appendix_e_ingestion.py` still looks like cross-lane / NET_NEW baggage, but it is referenced by `scripts/run_wave1.py`. It was intentionally not deleted in this branch because that would have been scope creep and needed its own breakage analysis.

### C. PR stack handling

Because PR `#19` includes the PEP 639 fix already:

- owner can likely merge `#19` and then close or reevaluate `#18`
- if another agent is asked to clean the stack, check `main` first and do not assume both PRs should merge as-is

## 8. Read-First Files for the Next Agent

If resuming the package/investor-readiness track from this branch:

1. `README.md`
2. `pyproject.toml`
3. `proofs/artifacts/operations/20260425T125111Z_wave15b_handover.md` (this file)
4. PR `#19` body

If resuming the sovereign Phase 06 track instead:

1. `proofs/reruns/2026-03-21_phase06_contract_freeze_attempt_v3/missing_inputs_packet.json`
2. `proofs/phase06_inputs/series_gap_matrix.csv`
3. `proofs/artifacts/real_market_benchmarks/ft_c004_truth_requirements.json`
4. recovery startup prompt and recovery manifest noted below

## 9. Recovery Authorities Already Verified

These were verified during prior recovery work and rechecked during this session:

- GitHub recovery branch exists:
  - branch: `codex/ft-remote-recovery-2026-04-24`
  - head: `21d119386870e409dded44015beca67468d38cdd`
- Hugging Face dataset recovery artifacts exist:
  - dataset: `Zer0pa/ZPE-FT-artifacts`
  - verified paths:
    - `recovery/2026-04-24/RECOVERY_MANIFEST.md`
    - `recovery/2026-04-24/startup-prompts/ZPE-FT_STARTUP_PROMPT_2026-04-24.md`

If a new agent is starting cold from a fresh machine, that recovery branch and HF dataset are the right recovery anchors, not this chat transcript.

## 10. Recommended Next Actions

### If the owner’s immediate goal is merge / portfolio hygiene

1. Review and merge PR `#19`
2. Reassess PR `#18` for supersession
3. Leave Phase 06 status unchanged in any public or portfolio rollup

### If the owner’s immediate goal is sovereign lane progress

1. Move back to Phase 06 authority closure
2. Re-read the Phase 06 blocker artifacts
3. Decide whether to:
   - authorize the exact authority-source path and fetch the missing authority pack
   - explicitly rewrite the benchmark contract
   - kill the lane

Do not let this Wave 1.5b pass be substituted for that decision.

## 11. Resume Commands

From a fresh clone:

```bash
git clone https://github.com/Zer0pa/ZPE-FT.git
cd ZPE-FT
git fetch origin
git checkout chore/wave-1-5b-investor-readiness-2026-04-25
```

To inspect the exact PR state:

```bash
gh pr view 19 --repo Zer0pa/ZPE-FT
gh pr checks 19 --repo Zer0pa/ZPE-FT
```

To rerun the main local verification surface:

```bash
python3.11 -m venv .venv-wave15b
. .venv-wave15b/bin/activate
python -m pip install --upgrade pip
python -m pip install -e '.[dev]'
python -m pytest -q
python -m build
```

## 12. Final State at Handover

- branch pushed: yes
- PR open: yes
- PR checks green: yes
- local worktree dirty: no
- sovereign Phase 06 closed: no

